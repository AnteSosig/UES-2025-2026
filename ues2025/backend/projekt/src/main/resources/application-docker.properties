# Docker-specific configuration
# This file is used when running in Docker containers

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://mysql:3306/svtkvt}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
logging.level.org.hibernate.SQL=debug
jwt.secretKey=smosmaKeysmosmimmnogojakololsmosmaKeysmosmimmnogojakololsmosmaKeysmosmimmnogojakololsmosmaKeysmosmimmnogojakololsmosmaKeysmosmimmnogojakolol

# Elasticsearch Configuration
elasticsearch.host=${ELASTICSEARCH_HOST:elasticsearch}
elasticsearch.port=${ELASTICSEARCH_PORT:9200}
spring.elasticsearch.uris=${SPRING_ELASTICSEARCH_URIS:http://elasticsearch:9200}

# MinIO Configuration
minio.endpoint=${MINIO_ENDPOINT:http://minio:9000}
minio.accessKey=${MINIO_ACCESSKEY:minioadmin}
minio.secretKey=${MINIO_SECRETKEY:minioadmin}
minio.bucketName=${MINIO_BUCKETNAME:uesgaming}

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
